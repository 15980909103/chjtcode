const install = (Vue, vm) => {
	//console.log(Vue, vm)

	// Vue.config.optionMergeStrategies.onLoad = (parent,child)=>{
	// 	//第一个参数父 
	// 	//第二个参数指的是子  
	// 	if (child) {  //如果子中有,返回子的
	// 		vm.onLaunched().then(res=>{
	// 			console.log(child)
	// 			//child.apply(child)
	// 		});
	// 		console.log(222)
	// 		return Array.isArray(child) ? child : [child];
	// 	}
	// 	if (parent) {  //如果父有,返回父的
	// 		return parent;
	// 	}
	// 	//都没有的时候生成一个
	// 	return [function(){ }]
	// }
}

export default {
	install
}